/**
 * Vertical exaggeration applied to modelled water depth on the 3D map so that a
 * decimetre-scale sheet of water is visible at province zoom. The UI states this
 * factor next to the depth readout so nobody reads the render as a real level.
 */
export const DEPTH_EXAGGERATION = 25;
